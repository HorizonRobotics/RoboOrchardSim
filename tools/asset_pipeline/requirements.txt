# Core dependencies
trimesh>=3.20.0
numpy>=1.24.0
scipy>=1.10.0
Pillow>=9.5.0

# Headless GPU rendering (replaces pyrender — no X11/EGL needed)
torch>=2.0.0
nvdiffrast>=0.4.0

# GPT client
openai>=1.0.0
tenacity>=8.2.0
PyYAML>=5.1

# Convex decomposition
coacd

# USD support (optional - only needed for .usd/.usda/.usdc/.usdz files)
# usd-core>=24.0
